name = "bobzhang/ldiff"

version = "0.1.0"

import {
  "moonbitlang/lexer@0.3.11",
}

readme = "README.md"

license = "Apache-2.0"

keywords = [ "diff", "lexer", "html" ]

description = "Lexer-based diff for MoonBit code: weighted token alignment with HTML rendering"

source = "."
